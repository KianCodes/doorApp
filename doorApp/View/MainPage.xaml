<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"  
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"  
             xmlns:view="clr-namespace:doorApp.View;assembly=doorApp"
             x:Class="doorApp.View.MainPage"
             Title="DOOR Main Menu">
    <StackLayout>
        <Button
                    BackgroundColor="Bisque"
                    Text="Device List" 
                    TextColor="Black"
                    Command="{Binding NavigateCommand}" CommandParameter="{x:Type view:OldDeviceList}"
                    />
        <Button 
                    BackgroundColor="BurlyWood"
                    Text="New Page" 
                    TextColor="Black"
                    Command="{Binding NavigateCommand}" CommandParameter="{x:Type view:Page2}"
                    />
        <Button 
                    BackgroundColor="DarkSlateBlue"
                    Text="Other New Page"
                    TextColor="Black"
                    Command="{Binding NavigateCommand}" CommandParameter="{x:Type view:Page3}"
                    />
    </StackLayout>
</ContentPage> 